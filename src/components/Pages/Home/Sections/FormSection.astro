---
import Container from "@/layouts/Container.astro";
import { ContactForm } from "../ContancForm";
import { wpFetch } from "@/lib/wp";

const { fields } = Astro.props;
const { acf } = await wpFetch("/acf/v3/options/options");

---

<section id="form" class="py-10 my-10 lg:my-[75px] lg:py-[75px] w-full h-[80vh]"
style={`background: url(${fields.image}) no-repeat center center / cover;`}>
  <Container>
    <div class="grid relattive">
      <!-- <Image
        src={fields.image}
        alt="форма консультации"
        width={500}
        height={700}
        class="absolute rounded-2xl ml-auto w-full h-auto object-cover shadow-md"
      /> -->
      <ContactForm {fields} {acf} client:load />
    </div>
  </Container>
</section>
